# BTG — Bio-Telemetry Gateway

> ⚠️ **Alpha 版本**：本项目处于早期开发阶段，接口与行为可能随时变更，请勿在生产环境依赖。

基于 ARM 开发板（瑞芯微 / 树莓派等）的**人体状态监控 + LLM 驱动的设备调整系统**。

BTG 的核心玩法是：通过**摄像头（视觉识别）配合心率监控设备**（心率带、小米手环
蓝牙广播、BLE 心率传感器等）**实时感知人体状态**——摄像头捕捉表情、姿态与挣扎反应，
心率设备追踪生理波动，两者在时间轴上对齐融合；再由 **LLM（大语言模型）根据预设
剧本 / 工作流 / 实时生理数据**，动态控制 **DG-LAB 郊狼（Coyote）、役次元（Yokonex）、
Buttplug 生态**等电刺激 / 振动设备，形成「感知 → 决策 → 反馈」的赛博闭环。

系统采用**边缘 + 云端管理后台**架构，预留传感器 / 识别器 / 控制器插件接口，支持第三方二开。

---

## ⚠️ 免责声明（请务必阅读）

> 使用本项目即表示你已阅读并同意以下全部条款。

### 1. 人身安全风险
- 本项目可控制**电刺激（TENS/EMS）设备**（如 DG-LAB 郊狼、役次元等）。电刺激设备可能对
  心脏起搏器佩戴者、孕妇、癫痫患者、皮肤敏感者及患有心脑血管疾病的人群造成严重伤害甚至死亡。
- **心脏起搏器佩戴者、孕妇、未成年人严禁使用**。使用前请咨询专业医生。
- 电极片请勿放置在**头部、颈部、胸部（心脏区域）、生殖器及口腔**等敏感部位。
- 若出现疼痛、麻木、灼伤、皮肤过敏或任何不适，请**立即停止**并移除设备。

### 2. 非医疗设备
- 本项目**不是医疗器械**，不提供任何医疗诊断、治疗或监护功能。
- 心率、IMU 等生理数据仅用于娱乐场景的自动化控制，**不可**作为任何医疗判断依据。
- 请勿在驾驶、操作机械、游泳、洗澡等需要保持注意力或存在触电风险的场景使用。

### 3. AI / LLM 不可控性
- 本项目的核心决策由 **LLM 自主生成**。大语言模型的输出**具有不可预测性**，
  即使配置了安全限幅、看门狗、硬急停等机制，也**无法保证绝对安全**。
- 内置安全机制（软降级 / 硬急停 / 黑盒审计）是**尽力而为的缓解措施**，而非安全保证。
- 请始终在**清醒、自愿、有他人监护**的状态下使用，并保留随时手动断开设备的权利。

### 4. 设备厂商协议合规
- 接入 DG-LAB 郊狼等设备时，请遵守设备厂商的**官方协议授权条款**（DG-LAB 官方蓝牙协议
  禁止未经授权的商业使用）。本项目代码以 MIT 开源，**不代表**设备厂商对本项目的背书或授权。
- 请自行确认你所在地区对相关设备的法律法规。

### 5. 使用者责任与合法用途
- 本项目**仅供成年人**在自愿、知情、合法的前提下用于娱乐与个人研究。
- **严禁**用于伤害他人、骚扰、非法监控、医疗欺诈或任何违反当地法律的行为。
- 使用者需对自身及他人的安全负全部责任。

### 6. 开源免责
- 本项目以 **MIT 协议**开源，按「现状」提供，**不附带任何明示或暗示的担保**，
  包括但不限于适销性、特定用途适用性及不侵权保证。
- 作者与贡献者**不对**因使用本项目造成的任何直接或间接损害承担责任。

### 7. 数据隐私
- 本项目会采集摄像头画面、麦克风音频、心率等**敏感生物数据**，请仅在本地 / 私有网络部署。
- 若接入第三方 LLM 服务，请注意提示词与生理数据可能被发送至第三方，请自行评估隐私风险。

---

## ✨ 核心特性

### 📷 多模态人体感知（Camera + Heart Rate）
- **摄像头视觉识别**：表情、姿态、挣扎反应、痛苦指数（NPU 加速，支持瑞芯微 RKNN / Hailo）。
- **心率监控**：BLE 心率带、小米手环蓝牙广播、心率传感器实时追踪 BPM 与突变率（delta_bpm）。
- **时间轴对齐融合**：视觉与生理数据按时间戳对齐，由融合引擎综合判定人体状态。
- **冗余设计**：传感器支持主备冗余路由，单路失效自动切换，保证感知链路可靠。

### 平台内核（Platform Kernel）
- **插件化模块架构**：传感器 / 执行器 / 数据源 / 代理 / 扩展模块统一经 `btg.plugins`
  entry-point 双轨加载（内置包 + 第三方包），支持热插拔与二开。
- **硬件抽象层（HAL）**：逻辑通道配置、冗余路由（主备设备自动故障切换）。
- **安全沙箱（Safety）**：数值截断（Clamp）、看门狗（Watchdog）、分级安全闸（Guardrail）。
- **多模态融合（Fusion）**：聚合高频时间序列，规则引擎触发状态机迁移。
- **事件总线**：进程内异步发布 / 订阅，解耦系统各层。
- **配置中心**：REST API 动态调整核心运行参数并热更新。

### 🛡️ 动态风控与自适应安全边界（Guardrail & Safety Dashboard）
- **软降级**：AI 激进出力或心率达预警线时触发限幅器（×衰减系数），而非直接切断。
- **硬急停**：WebSocket 心跳掉线、心率连续超限或 IMU 剧烈挣扎时，内核绕过上层直接下发硬件归零指令。
- **黑盒审计**：环形缓冲保留最近 1 小时状态帧，每条带时间戳与因果链指针（`原因 ➔ 动作 ➔ 结果`）。

### 🔗 动态设备工作流编排器（Visual Node-Based Automation）
Node-RED 极简版：**触发节点**（心率阈值 / 突变率、视觉痛苦指数、执行器反馈、手动触发）、
**条件节点**（AND / OR / 阈值比较）、**动作节点**（设置通道强度 / 物理位置 / 强制 AI 话术）。
前端拖拽连线导出 JSON，后端图遍历解释器按 Tick（默认 5Hz）实时执行。

### 🎭 动态剧本 / 人格切换器（Persona & Scenario Marketplace）
一个剧本包 = System Prompt + 硬件映射策略（`heart_rate_multiplier` /
`allow_ai_full_control` / `max_allowed_intensity`）。一键切换 AI 人格与硬件策略，
社区工坊支持从远端 API 拉取剧本。

### 🧬 历史心率与惩罚复盘曲线（Timeline & Replay / Session Log）
赛车级 Telemetry 会话回放：同一时间轴渲染**生理指标 / 硬件状态 / AI 动作**三条平行轨道，
支持导出含 SVG 图表 + 完整 JSON 黑盒日志的压缩包。

---

## 🏗️ 架构总览

```
┌─────────────────────────────────────────────────────────────┐
│  边缘端（ARM 开发板）                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ 传感器插件     │  │ 识别器插件     │  │ 控制器插件        │   │
│  │ 摄像头/心率带   │  │ 视觉+生理融合   │  │ 郊狼/役次元/API   │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
└──────────────────────────┬──────────────────────────────────┘
                           │ WebSocket / REST
┌──────────────────────────▼──────────────────────────────────┐
│  云端管理后台（BTG Gateway）                                  │
│  Platform Kernel → HAL / Safety / Fusion / Bus / Integration │
│  + LLM Agents / Workflow / Persona / Replay / Story          │
│  LLM 决策 → 安全闸 → 执行器下发                                │
└──────────────────────────────────────────────────────────────┘
```

## 📁 目录结构

```
├── backend/                 # BTG 网关后端（FastAPI + 平台内核）
│   └── src/btg/
│       ├── platform/        # 平台内核：manifest / module / registry / loader / kernel
│       ├── hal/             # 硬件抽象层：传感器 / 执行器 / 冗余路由
│       ├── safety/          # 安全层：clamps / watchdog / guardrail / policy
│       ├── fusion/          # 多模态融合引擎
│       ├── bus/             # 总线层：REST 端点 + WebSocket 遥测流
│       ├── integration/     # 第三方接入：inbound / outbound / webhook
│       ├── agents/          # LLM 代理：game / llm_master / scenario
│       ├── story/           # 剧情导入与场景执行引擎
│       ├── workflow/        # 工作流编排器
│       ├── persona/         # 剧本人格市场
│       ├── replay/          # 复盘曲线（会话录制 + 报告导出）
│       └── config/          # 配置中心与热更新引擎
├── sdk/                     # btg_sdk：第三方插件 SDK 核心基类
├── board_agent/             # ARM 板端代理（采集 / 识别 / 控制）
├── frontend/                # Vue 3 + Vite + Tailwind 控制台
├── btg-nexus/               # 备用 Vue 前端
├── bio-telemetry-console/   # 静态 HTML 控制台（设计资产）
├── config/                  # 运行时配置（*.example.yaml 提交，实际配置忽略）
├── docs/                    # 架构文档
└── tests/                   # 全量测试
```

## 🚀 快速开始

> Alpha 版本：以下命令仅用于本地开发调试，请勿在无人监护的环境运行。

### 后端

```bash
cd backend
python -m venv .venv && .venv\Scripts\activate   # Windows
pip install -e .[dev]
python -m btg.gateway                            # 启动网关（默认 :8000）
```

### 前端

```bash
cd frontend
npm install
npm run dev
```

开发模式默认把 `/api`、`/integration`、`/ws` 代理到 `http://localhost:8000`。

### 测试

```bash
python -m pytest tests/
```

## 📄 文档

- [架构文档](docs/architecture.md)：平台内核契约、模块开发指南、扩展模块说明。

## 📜 License

[MIT](LICENSE) © BTG Contributors

---

再次提醒：**请在阅读并同意上方免责声明后使用本项目。安全第一，量力而行。**
